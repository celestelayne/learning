<h1><%= @user.name %> and his links!</h1>

<ul>
<% @user_links.each do |link| %>
 <li> <%= link_to link.shortened_url, root_path + link.shortened_url%> </li>
 <!-- <%= button_to "Destroy!!!", urls_url(link), :method => :delete %> -->

<%= button_to "delete", {:controller => :urls,
      :action => 'destroy', :id => link.id }, :method => :delete %>
 
<%= link_to %>

<% end %>
</ul>

